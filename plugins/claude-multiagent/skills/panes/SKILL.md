---
name: panes
description: Reopen accidentally closed dashboard panes
---

# Panes

Reopen missing dashboard panes. Just run:
```bash
"${CLAUDE_PLUGIN_ROOT}/scripts/open-dashboard.sh"
```
The script auto-detects which panes are missing and creates only those.
